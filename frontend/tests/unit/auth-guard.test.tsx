import { describe, expect, it } from "vitest";

describe("requireAuth", () => {
  it("should be covered by integration tests that redirect unauthenticated users", () => {
    expect(true).toBe(true);
  });
});
